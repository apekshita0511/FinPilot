# Copy to apps/api/.env for running the API locally.

NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://finpilot:changeme@localhost:5432/finpilot?schema=public

# Any random string of at least 32 characters.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-random-32-plus-character-string

WEB_ORIGIN=http://localhost:5173
